{
  "name": "@pretext-epub/demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@pretext-epub/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^1.6.1"
  }
}
